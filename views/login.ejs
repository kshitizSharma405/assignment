<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <!-- Link to external CSS for styling -->
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <!-- Include header partial that contains navigation links -->
    <%-include ('partials/header.ejs') %>

    <div class="container">
      <div class="login-form">
        <h2>Login</h2>

        <!-- Login form with POST method to send data to the server -->
        <form action="/api/account/login" method="POST">
          <label for="username">Username or Email</label>
          <input type="text" name="username" id="username" required />

          <label for="password">Password</label>
          <input type="password" name="password" id="password" required />

          <!-- Google reCAPTCHA widget to verify the user -->
          <div class="g-recaptcha" data-sitekey="<%= siteKey %>"></div>

          <button type="submit">Login</button>
        </form>

        <% if (error) { %>
        <!-- Display error message if login fails -->
        <div class="error-message">
          <p><%= error %></p>
        </div>
        <% } %>

        <!-- Link to the registration page for users without an account -->
        <a href="/api/account/register">Don't have an account? Register here</a>
      </div>
    </div>

    <!-- Include footer partial -->
    <%- include('partials/footer.ejs') %>

    <!-- Google reCAPTCHA API script -->
    <script src="https://www.google.com/recaptcha/api.js" async defer></script>
  </body>
</html>
